#include<iostream>
#include<vector>
using namespace std;
typedef long long ll;
int n;
vector<ll> A;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cin>>n;
	A.resize(n);
	for(int i =0;i<n;i++){
		cin>>A[i];
	}
	ll a=0;
	for(int i =0;i<n;i++){

	}
	return 0;
}

